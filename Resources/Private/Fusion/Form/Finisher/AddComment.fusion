
prototype(Breadlesscode.Blog:From.Finisher.AddComment.Definition) < prototype(Neos.Form.Builder:Finisher.Definition) {
    options {
        // form values
        email = ''
        name = ''
        content = ''
        // the node where the comments are stored
        node = null
    }

    formElementType = 'Breadlesscode.Blog:From.Finisher.AddComment'
}
